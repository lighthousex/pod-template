source 'https://github.com/caoer/Specs.git'
source 'https://github.com/CocoaPods/Specs.git'

use_frameworks!
platform :ios, '10.1'

target '${POD_NAME}_Example' do
  pod '${POD_NAME}', :path => '../'

  pod 'LHXCore', git: 'https://github.com/caoer/LHXCore'

  target '${POD_NAME}_Tests' do
    inherit! :search_paths

    ${INCLUDED_PODS}
  end
end

target '${POD_NAME}_PatcherGenerator' do
  pod 'AceFileGenerator', git: 'https://github.com/caoer/AceFileGenerator'
end

post_install do |installer|
  if File.exist?('../AceFileGenerator-template')
    `cd ../AceFileGenerator-template; git pull;`
  else 
    `git clone https://github.com/caoer/AceFileGenerator-template ../AceFileGenerator-template`
  end

  if File.exist?('../AceFileAutoFix')
    `cd ../AceFileAutoFix`
  else
    `git clone https://github.com/caoer/AceFileAutoFix ../AceFileAutoFix`
  end
end